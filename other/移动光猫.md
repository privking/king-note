# 移动光猫

**UNG953H1-S**

1：先查看光猫背面的用户名和密码。

2：浏览器输入http://192.168.1.1/webcmcc/telnet.html即可开启成功。
3：使用telnet登录光猫。输入光猫背面标签上的USER的用户名和密码登录telnet。
4:1.cd /config/workb 然后再输入ls再按回车键 一般会出现backup_lastgood.xml文件。
  2.输入grep -i -n "admin" backup_lastgood.xml来查询所有账号的用户名。
  3.输入grep -i -n "password" backup_lastgood.xml来查询所有账号的密码。





打开http://192.168.1.1/  
不用登录 (这猫安全性也太差了，不用登录就可以添加root用户)
按F12打开控制台
不知道怎么打开控制台的，百度搜索 "f12 控制台" 会有图文教程
输入以下代码（代码全部复制）：

var obj = {};
obj.telnet_lan_enabled = '1';
obj.telnet_wan_enabled = '0';
obj.user_name = encrypt_info("root");
obj.user_password = encrypt_info("router@001");
obj.telnet_port = '23';
sendRequest("/boaform/set_telnet_enabled",obj,function(data){location.reload();});

复制黏贴后回车，可以看到页面好像刷新了一下，就成功了。
这样添加的用户是永久的，除非恢复出厂设置
telnet 192.168.1.1 用户名root 密码router[url=home.php?mod=space&uid=152327]@001[/url] (以上代码里面的用户名和密码，可以随意修改成自己喜欢的密码)
